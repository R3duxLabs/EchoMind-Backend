services:
  - type: web
    name: echomind-backend
    env: python
    buildCommand: ./install_deps.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: PORT
        value: 10000